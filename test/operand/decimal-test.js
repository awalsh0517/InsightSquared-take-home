'use strict';

const expect = require('chai').expect;
const { isValidOperand, Decimal } = require('../../operand/decimal')

describe('basic tests for decimal values', function () {
  describe('determine  operand is a decimal', function () {
    it('should be a decimal', function () {

      // expect(isValidOperand(55)).to.equal(true);
    });
  });

  // Would want to check error path

  describe('check to see if input is an integar', function () {
    it('', function () {

    });
  });

});
