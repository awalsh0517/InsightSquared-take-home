'use strict';
// const { isValidOperand, Binary } = require('../../operand/binary')
// const expect = require('chai').expect;


describe('basic tests for binary values', function () {
  // Check to see if binary coversion works

  describe('check to see if binary coversion works', function () {
    it('should convert an input with the format of base 2 or 0b to a base 10 integar', function () {
    });
  });

  // Check to see if an error is thrown when a non 1 or 0 is put in.

  describe('check to see if an error is thrown when a non 1 or 0 is put in', function () {
    it('should return an error when an input with the format of base 2 is followed by the integars 2-9', function () {
      // expect(new Binary)(isValidOperand('0b121')).to.equal(false)

    });
  });
});
